{
  "name": "skyrim-gemsse",
  "version": "1.0.0",
  "description": "",
  "repository": "",
  "scripts": {
    "start": "SET env=PROD & pug src/pages -o docs --pretty -O options.js",
    "dev": "SET env=DEV & nodemon --watch src -e js,pug,json --exec \"pug src/pages -o dev --pretty -O options.js\""
  },
  "keywords": [
    "pug",
    "pug-cli",
    "node",
    "marked"
  ],
  "author": "Big Duckie",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "marked": "^4.0.5",
    "pug": "^3.0.2",
    "pug-cli": "^1.0.0-alpha6"
  }
}
